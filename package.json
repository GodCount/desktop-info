{
  "name": "desktop-info",
  "version": "0.1.0",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "desktop-info",
    "triples": {}
  },
  "license": "MIT",
  "devDependencies": {
    "@napi-rs/cli": "^2.13.0",
    "@types/jest": "^29.2.3",
    "jest": "^29.3.1"
  },
  "engines": {
    "node": ">= 10"
  },
  "scripts": {
    "build": "napi build --platform --release --js ./build/release/index.js --dts ./build/release/index.d.ts && node ./scripts/build.js build",
    "build:debug": "napi build --platform",
    "test": "jest",
    "version": "napi version"
  },
  "packageManager": "yarn@3.3.0"
}
